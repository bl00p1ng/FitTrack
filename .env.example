# Copy this file to `.env` and fill in the secrets before running the app.
# SECRET_KEY is used by Flask to sign sessions and CSRF tokens.
SECRET_KEY=replace-with-a-secure-random-value

# Optional: uncomment and customise the database URL if you are not using the default.
# DATABASE_URL=postgresql+psycopg://username:password@localhost:5432/fittrack_db
